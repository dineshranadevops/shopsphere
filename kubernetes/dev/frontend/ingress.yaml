apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend
  namespace: shopsphere
spec:
  ingressClassName: nginx
  rules:
    - host: shopsphere.akcomputer.in
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
